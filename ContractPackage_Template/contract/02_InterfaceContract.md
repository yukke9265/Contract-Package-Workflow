# インターフェース契約

## 1. シグネチャ定義

```cpp
[ここに include/[PublicHeader] と一致する宣言を記述する]
```

## 2. 引数と戻り値

### 2.1 引数

| 名前 | 型 | 必須 | 形式 / 単位 | 制約 | 説明 |
| --- | --- | --- | --- | --- | --- |
| [arg1] | [type] | [Y/N] | [InputFormat] | [InputConstraint] | [意味] |

### 2.2 戻り値

| 名前 | 型 | 条件 | 説明 |
| --- | --- | --- | --- |
| [result] | [StatusType or value type] | [正常時 / 失敗時] | [意味] |

## 3. 所有権と寿命

- 引数の所有権: [借用 / 共有 / 移譲]
- 戻り値の所有権: [呼び出し側に移譲 / 内部所有参照]
- 参照やポインタの有効期間: [条件]
- コピー可否: [可 / 不可 (=delete)]
- コピーコスト: [O(1) / O(n) / ディープコピー / 該当なし]
- move後状態: [有効だが未規定 / 空状態 / move不可]

## 4. 無効化規則

- 参照 / ポインタ / iterator / view が無効になる条件: [InvalidationRule]
- 取得済みハンドルの再利用可否: [可 / 不可 / 条件付き]
- キャッシュしてよい値: [何を保持してよいか]

## 5. ライフサイクル

- 生成条件: [デフォルト構築可 / ファクトリ経由のみ / 初期化API必須]
- 初期状態: [直後に何ができるか]
- 呼び出し順制約: [LifecycleRule]
- 破棄時副作用: [なし / リソース解放 / 通知送出]
- 再利用可否: [可 / 不可 / Reset後のみ可]

## 6. 事前条件

- [呼び出し前に満たすべき条件]
- [条件を満たさない場合の扱い → `03_ErrorModel.md` に従う]

## 7. 事後条件

- [成功時に必ず成り立つ条件]
- [失敗時に保証される条件]

## 8. 副作用

| 種別 | あり / なし | 内容 |
| --- | --- | --- |
| データ更新 | [あり / なし] | [更新対象] |
| 外部I/O | [あり / なし] | [ファイル / ソケット / DB / なし] |
| コールバック / 通知 | [あり / なし] | [対象と条件] |
| ログ出力 | [あり / なし] | [レベルと内容] |

## 9. 並行性

- 同一インスタンスの同時呼び出し可否: [可 / 不可 / 条件付き]
- read-read 同時実行: [可 / 不可]
- read-write 同時実行: [可 / 不可]
- 排他要件: [必要なら記載]
